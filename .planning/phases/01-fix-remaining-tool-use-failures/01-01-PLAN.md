---
phase: 01-fix-remaining-tool-use-failures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Tier1-AIClient2API/src/utils/request-handlers.js
  - Tier2-LiteLLM/litellm_config.yaml
autonomous: false
requirements:
  - REQ-05
must_haves:
  truths:
    - "Gemini context window injection change is committed to git"
    - "LiteLLM health endpoint returns healthy on curl"
    - "Claude Code session makes requests that appear in LiteLLM logs (port 4000)"
    - "No SSE stream corruption visible when streaming a response through port 4000"
  artifacts:
    - path: "Tier1-AIClient2API/src/utils/request-handlers.js"
      provides: "Gemini context window injection (already edited, needs commit)"
    - path: "Tier2-LiteLLM/litellm_config.yaml"
      provides: "SSE buffering settings under litellm_settings"
  key_links:
    - from: "~/.claude/settings.json env block"
      to: "http://127.0.0.1:4000"
      via: "ANTHROPIC_BASE_URL value"
      pattern: "4000"
    - from: "LiteLLM :4000"
      to: "AIClient2API :3000"
      via: "api_base in each model entry"
      pattern: "127.0.0.1:3000"
---

<objective>
Commit the pending Gemini context window fix, add SSE buffering settings to LiteLLM config,
and restore ANTHROPIC_BASE_URL to route through Tier 2 (:4000).

Purpose: Without Tier 2 in the request path, LiteLLM's retry/fallback/context-window-fallback
logic never fires. The Tier 2 bypass was a temporary workaround for SSE corruption; the real
fix is configuring LiteLLM to disable nginx-style response buffering, not bypassing the tier.

Output:
- Git commit for the Gemini context window change in request-handlers.js
- litellm_config.yaml updated with stream_timeout and response header settings
- User-facing instruction for updating ~/.claude/settings.json ANTHROPIC_BASE_URL
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tool_failure_root_cause.md
</context>

<interfaces>
<!-- Key facts for the executor. No codebase exploration needed. -->

Current state of litellm_config.yaml (lines 580-588):
```yaml
litellm_settings:
  drop_params: false
  set_verbose: false

general_settings:
  port: 4000
```

The SSE buffering fix adds these keys to litellm_settings:
```yaml
litellm_settings:
  drop_params: false
  set_verbose: false
  stream_timeout: 600          # Keep streaming connections alive up to 10 min
  request_timeout: 600         # Match stream timeout to avoid premature closes
  response_headers:
    X-Accel-Buffering: "no"    # Disable nginx proxy buffering on SSE streams
```

CRITICAL constraint on ~/.claude/settings.json:
- CLAUDE.md rule: "Only claude-proxy/claude-native should modify this. Do not edit manually."
- The executor MUST NOT directly edit ~/.claude/settings.json.
- Instead: present the exact JSON diff to the user and instruct them to run:
    claude-proxy   (which calls ~/AIClient2API/scripts/claude-mode.sh and sets ANTHROPIC_BASE_URL=http://127.0.0.1:3000)
  Wait — that sets :3000. The user needs :4000. So the correct approach is:
  - Run `claude-mode-status` to show current value
  - Provide the exact one-liner the user should run to update settings.json to :4000,
    OR update the shell function in ~/dotfiles/zsh/zshrc so claude-proxy sets :4000 instead of :3000.
  - The dotfiles zshrc IS editable (it is not in the never-touch list for this specific change).
    Check ~/AIClient2API/scripts/claude-mode.sh for where the URL is hardcoded.

Gemini context window change location:
- File: Tier1-AIClient2API/src/utils/request-handlers.js
- This file has been modified (shown in git status as "M") but not committed.
- The change injects the correct 1M context window for Gemini models.
- Commit it independently before touching litellm_config.yaml.

Git rules from CLAUDE.md:
- Never `git add -A` or `git add .`
- Always specify files explicitly
- Only commit when instructed (this task explicitly instructs it)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Commit pending Gemini context window fix</name>
  <files>Tier1-AIClient2API/src/utils/request-handlers.js</files>
  <read_first>
    - Run: git diff Tier1-AIClient2API/src/utils/request-handlers.js
      Reason: Must understand exactly what changed before committing. The diff shows the
      Gemini 1M context window injection. Confirm it only touches context window logic
      and nothing else before proceeding.
  </read_first>
  <action>
1. Run `git diff Tier1-AIClient2API/src/utils/request-handlers.js` and read the output.
2. Confirm the diff contains only Gemini context window injection changes (MODEL_CONTEXT_WINDOWS
   additions or GEMINI_DEFAULT_MAX_TOKENS adjustments). If the diff contains anything else,
   stop and report what was found — do not commit mixed changes.
3. If the diff is clean, stage only that file:
   `git add Tier1-AIClient2API/src/utils/request-handlers.js`
4. Commit with message:
   `fix(gemini): inject 1M context window for Gemini models in MODEL_CONTEXT_WINDOWS`
5. Run `git log --oneline -3` to confirm the commit appears.
  </action>
  <verify>
    <automated>git log --oneline -3 | grep -i "gemini\|context.*window\|1M"</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -3` output contains a commit with "gemini" or "context window" in the message
    - `git status` shows Tier1-AIClient2API/src/utils/request-handlers.js as clean (no M marker)
    - `git diff HEAD~1 Tier1-AIClient2API/src/utils/request-handlers.js` shows the Gemini context change
  </acceptance_criteria>
  <done>Gemini context window fix committed; working tree clean for that file.</done>
</task>

<task type="auto">
  <name>Task 2: Add SSE buffering settings to LiteLLM config</name>
  <files>Tier2-LiteLLM/litellm_config.yaml</files>
  <read_first>
    - Read lines 578-590 of Tier2-LiteLLM/litellm_config.yaml
      Reason: Must see the exact current text of litellm_settings block before editing.
      The Edit tool requires exact byte matching.
  </read_first>
  <action>
Edit Tier2-LiteLLM/litellm_config.yaml: replace the current litellm_settings block:

CURRENT (lines 580-582):
```yaml
litellm_settings:
  drop_params: false
  set_verbose: false
```

REPLACE WITH:
```yaml
litellm_settings:
  drop_params: false
  set_verbose: false
  stream_timeout: 600
  request_timeout: 600
  response_headers:
    X-Accel-Buffering: "no"
```

DO NOT touch any other part of the file. The `general_settings` block below must remain
unchanged. Do not add blank lines around the new keys beyond what the YAML requires.

After editing, run:
  grep -A 8 "^litellm_settings:" Tier2-LiteLLM/litellm_config.yaml
to confirm the exact text was written correctly.
  </action>
  <verify>
    <automated>grep -A 8 "^litellm_settings:" Tier2-LiteLLM/litellm_config.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A 8 "^litellm_settings:"` output contains all five lines:
        drop_params: false
        set_verbose: false
        stream_timeout: 600
        request_timeout: 600
        X-Accel-Buffering: "no"
    - No other lines in the file changed (verify with `git diff Tier2-LiteLLM/litellm_config.yaml | grep "^[-+]" | grep -v "^[-+][-+][-+]"`)
    - YAML is valid: `python3 -c "import yaml; yaml.safe_load(open('Tier2-LiteLLM/litellm_config.yaml'))"` exits 0
  </acceptance_criteria>
  <done>litellm_config.yaml updated with SSE buffering settings; YAML valid.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Restore ANTHROPIC_BASE_URL to :4000 and verify Tier 2 routing</name>
  <what-built>
    Tasks 1-2 committed the Gemini fix and configured LiteLLM for SSE buffering.
    This checkpoint restores the ANTHROPIC_BASE_URL so Claude Code routes through Tier 2.
  </what-built>
  <how-to-verify>
Step 1 — Check where ANTHROPIC_BASE_URL is currently set:
  grep -r "ANTHROPIC_BASE_URL" ~/AIClient2API/scripts/claude-mode.sh ~/dotfiles/zsh/zshrc 2>/dev/null

Step 2 — Find the line that sets :3000 in the proxy script and update it to :4000:
  The change must set ANTHROPIC_BASE_URL=http://127.0.0.1:4000 instead of :3000.
  Check ~/AIClient2API/scripts/claude-mode.sh — if it hardcodes :3000, change it to :4000.
  The file is not in the never-touch list for this targeted change.

  DEFENSIVE CHECK: Before running sed, confirm the pattern exists:
    grep -c ':3000' ~/AIClient2API/scripts/claude-mode.sh
  If this returns 0, the URL is either already :4000, set elsewhere (check zshrc output
  from Step 1), or uses a variable. In that case, do NOT run sed — inspect manually and
  identify the actual source of the :3000 value before making any change.

Step 3 — Restart both tiers in the correct order (Tier 1 first):
  cd ~/AIClient2API && npm start &amp;
  Wait 5 seconds, then:
  /Users/ilialiston/MASTER-C/Tier2-LiteLLM/.venv/bin/litellm \
    --config /Users/ilialiston/MASTER-C/Tier2-LiteLLM/litellm_config.yaml \
    --port 4000 &amp;

Step 4 — Verify both tiers healthy:
  curl -sf http://127.0.0.1:3000/health | jq .status
  curl -sf http://127.0.0.1:4000/health | jq .status

Step 5 — Run claude-proxy to activate the new URL:
  claude-proxy

Step 6 — Confirm the active URL:
  claude-mode-status

Step 7 — Open a new Claude Code session and send a simple message. Confirm in the
  LiteLLM log (stdout or /tmp/aiclient.log) that a request appears for port 4000.

Expected: Both health checks return {"status":"healthy"...}, claude-mode-status shows
http://127.0.0.1:4000, and LiteLLM log shows an incoming request.
  </how-to-verify>
  <resume-signal>Type "approved" when both tiers are healthy and requests route through :4000, or describe any issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code → LiteLLM :4000 | SSE streaming; buffering misconfiguration causes chunk reordering |
| LiteLLM :4000 → AIClient2API :3000 | Internal loopback; no auth risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | litellm_config.yaml | mitigate | Validate YAML after edit with python3 yaml.safe_load before restart |
| T-01-02 | Denial of Service | stream_timeout: 600 | accept | Long timeout prevents false timeouts on slow models; no external exposure |
| T-01-03 | Information Disclosure | claude-mode.sh URL change | accept | Loopback URL only; no credential exposure |
</threat_model>

<verification>
After checkpoint approval:

1. `curl -sf http://127.0.0.1:4000/health | jq .status` → "healthy"
2. `curl -sf http://127.0.0.1:3000/health | jq .status` → "healthy"
3. `claude-mode-status` output contains "4000"
4. `git log --oneline -3` shows the Gemini context commit
5. `grep "stream_timeout" Tier2-LiteLLM/litellm_config.yaml` → returns "stream_timeout: 600"
</verification>

<success_criteria>
- Gemini context window commit present in git log
- LiteLLM config has stream_timeout: 600, request_timeout: 600, X-Accel-Buffering: "no"
- Both :3000 and :4000 return healthy
- ANTHROPIC_BASE_URL routes through :4000
- Streaming a response through the new path shows no SSE corruption (chunks arrive in order)
</success_criteria>

<output>
After completion, create `.planning/phases/01-fix-remaining-tool-use-failures/01-01-SUMMARY.md`
</output>

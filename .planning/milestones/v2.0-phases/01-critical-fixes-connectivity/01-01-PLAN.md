---
plan: "01-01"
title: "Verify BUG-01 Fix + Disable openai-custom Pool"
wave: 1
depends_on: []
autonomous: true
files_modified:
  - AIClient2API/configs/provider_pools.json
requirements_addressed:
  - BUG-01
  - BUG-02
  - DIAG-04
  - OPS-02
must_haves:
  - "node --check src/providers/gemini/antigravity-core.js exits 0 (no output)"
  - "openai-custom account has isDisabled: true in configs/provider_pools.json"
  - "pnpm test exits 0 (all tests pass after config change)"
  - "curl http://127.0.0.1:3000/provider_health shows openai-custom as disabled/0-healthy"
---

# Plan 01-01: Verify BUG-01 Fix + Disable openai-custom Pool

## Objective

Confirm the antigravity-core.js SyntaxError (BUG-01) is resolved and document it.
Disable the openai-custom provider pool account (BUG-02) so it stops silently failing
in the fallback chain.

## Context

- BUG-01 is already fixed (`node --check` exits 0 as of v3.2.0 merge) — this plan VERIFIES it
- openai-custom pool has 1 account with `isDisabled` field already in schema
- Pool manager skips any account where `isDisabled === true` (provider-pool-manager.js:262)
- Config change in provider_pools.json takes effect on next proxy restart

## Tasks

<task id="01-01-T1" type="verify">
<title>Verify BUG-01: antigravity-core.js has no SyntaxError</title>
<read_first>
- AIClient2API/src/providers/gemini/antigravity-core.js (the file to syntax-check)
</read_first>
<action>
Run: node --check AIClient2API/src/providers/gemini/antigravity-core.js

Expected: exits 0 with no output.
If it fails: read the error, locate the actual syntax issue, fix it minimally, re-run node --check.
Document the outcome either way.
</action>
<acceptance_criteria>
- `node --check AIClient2API/src/providers/gemini/antigravity-core.js` exits 0 with no stdout/stderr output
- If a fix was needed: `pnpm test` still exits 0 after the fix
</acceptance_criteria>
</task>

<task id="01-01-T2" type="execute">
<title>Disable openai-custom account in provider_pools.json</title>
<read_first>
- AIClient2API/configs/provider_pools.json (full file — must read before editing to see current isDisabled value and account structure)
- AIClient2API/src/providers/provider-pool-manager.js (lines 260-265 — confirm isDisabled filter logic before relying on it)
</read_first>
<action>
In AIClient2API/configs/provider_pools.json, find the "openai-custom" array entry.
Set the account's `isDisabled` field to `true`.

The account structure is a flat array under the "openai-custom" key.
The field to set: `isDisabled: true` (already exists in the schema).

If the account already has `isDisabled: true`, no change needed — document it.
Do NOT edit configs/config.json or the providerFallbackChain entries.
</action>
<acceptance_criteria>
- `python3 -c "import json; d=json.load(open('AIClient2API/configs/provider_pools.json')); oc=d['openai-custom']; print(oc[0]['isDisabled'])"` prints `True`
- The JSON file remains valid (parse succeeds)
- No other provider entries are modified
</acceptance_criteria>
</task>

<task id="01-01-T3" type="verify">
<title>Run test suite to confirm no regressions</title>
<read_first>
- AIClient2API/package.json (confirm test script command)
</read_first>
<action>
Run: cd AIClient2API && pnpm test

This validates that the config change to provider_pools.json does not break any
existing unit or integration tests.
</action>
<acceptance_criteria>
- `pnpm test` exits 0
- Test count is ≥ 212 (the count from the last known-good run)
- No new test failures compared to baseline
</acceptance_criteria>
</task>

<task id="01-01-T4" type="verify">
<title>Verify /provider_health reflects openai-custom as disabled</title>
<read_first>
- AIClient2API/scripts/safe-restart.sh (understand restart procedure before running)
</read_first>
<action>
If the proxy is running:
  Run: ./AIClient2API/scripts/safe-restart.sh
  Then: curl -sf -H "Authorization: Bearer $AICLIENT_TOKEN" http://127.0.0.1:3000/provider_health | python3 -m json.tool

If the proxy is not running:
  Start it: cd AIClient2API && pnpm start &
  Wait 3s, then run the curl command above.

Look for openai-custom in the health output. It should show isHealthy: false or
isDisabled: true, and healthy count: 0.

NOTE: Restart must respect the 2GB RAM floor from safe-restart.sh — do not bypass.
</action>
<acceptance_criteria>
- Proxy starts without errors
- `curl http://127.0.0.1:3000/provider_health` returns valid JSON
- openai-custom entry shows 0 healthy accounts OR isDisabled: true
- Other providers (gemini-antigravity, github-models, claude-kiro-oauth) appear in the health output
</acceptance_criteria>
</task>

## Verification Criteria

All must_haves must be true before this plan is complete:

1. `node --check AIClient2API/src/providers/gemini/antigravity-core.js` → exit 0, no output
2. `configs/provider_pools.json` → openai-custom[0].isDisabled === true
3. `pnpm test` → exit 0, ≥212 tests pass
4. `/provider_health` → openai-custom shows 0 healthy accounts

## Notes

- If BUG-01 turns out to have a real SyntaxError (check reveals one): fix surgically at
  that specific line, do NOT refactor surrounding code, re-run node --check to confirm
- The providerFallbackChain in config.json still lists openai-custom — this is intentional.
  The pool manager handles the skip; the chain topology is unchanged (easier to re-enable later)
